import CloseIcon from "@mui/icons-material/Close"
import React, { memo } from "react"


export const ClearIcon = memo(function (): JSX.Element {
    return (<CloseIcon sx={{
        fontSize: '1.125rem'
    }} />)
})